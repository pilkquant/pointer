[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tinylib"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.0",
    "pyyaml",
]

[project.scripts]
tinylib = "tinylib.cli:main"
