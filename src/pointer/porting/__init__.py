"""Pointer porting engine — Codex-backed Python-to-Rust porting workflow.

This package implements the durable run state machine, agent backend protocol,
oracle capture, native build verification, bounded repair, and evidence reporting
for the `pointer port` command family.
"""
